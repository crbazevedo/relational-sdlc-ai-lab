# Data Area

This directory is for dataset cards, small public fixtures, and derived
metadata that can be redistributed.

Do not commit raw large datasets here. Use object storage for large records
and commit only source notes or dataset cards with:

- source repository or API;
- retrieval timestamp;
- license/API notes;
- transform command;
- content hash;
- split assignment;
- redistribution status.
